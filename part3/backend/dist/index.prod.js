"use strict";var app=require("./app"),http=require("http"),config=require("./utils/config"),logger=require("./utils/logger"),server=http.createServer(app);server.listen(config.PORT,function(){logger.info("Server running on port ".concat(config.PORT))});